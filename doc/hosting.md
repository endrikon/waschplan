# Hosting of static sites

# AWS
1. `S3` bucket for html and js storage.
2. `CloudFront` as a CDN -> `https://d[random].cloudfront.net`
3. potentially buy a domain name and re-route the traffic (`Route 53`)
